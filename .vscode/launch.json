{
   // Use IntelliSense to learn about possible attributes.
   // Hover to view descriptions of existing attributes.
   // For more information, visit: https://go.microsoft.com/fwlink/?linkid=830387
   "version": "0.2.0",
   "configurations": [

      {
         "type": "lldb",
         "request": "launch",
         "name": "CLI",
         "program": "${workspaceFolder}/target/debug/bt-bridge", 
         // "args": ["--my-arg", "value"],
         "cwd": "${workspaceFolder}",
         // "preLaunchTask": "cargo build" // cargo build -p bt-bridge-cli
      }

   ]
}